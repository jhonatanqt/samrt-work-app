import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'samrt-work-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
